#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int i = 97; i <= 122; i++)
	{
	putchar(i);
	}
	for (int x = 65; x <= 90; x++)
	{
	putchar(x);
	}
	putchar('\n');

	return (0);
}
