#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int i = 0; i <= 16; i++)
	{
	if ( i > 10 )
	{
		
	}
	putchar(i);
	}
	putchar('\n');

	return (0);
}
