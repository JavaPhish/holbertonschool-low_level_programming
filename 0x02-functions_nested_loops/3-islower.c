/**
 * _islower - prints whether or not a value is lower case
 * @c: the number to evaluate
 * Return: 0 (Uppercase)
 */
int _islower(int c)
{
	if (c > 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

